import React from "react";

// this is a funtional component
export default function FunctionalComponent() {
  return <div>FunctionalComponent</div>;
}

/**
 * A piece of code that can reuse
 * component has its own life cycle
 * 
 * there are multiple types  of component
 * => fucntional
 * => class
 * => pure
 * => controlled
 * => un-controlled
 * => HOC
 */

// only one export default in one file

// if need to export multiple export from one file then do this
// export {component1,component2}
